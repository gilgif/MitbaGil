# מדריך העלאה — מטבגיל

מדריך זה ילווה אותך צעד-אחר-צעד להעלאת האפליקציה לאינטרנט, כדי שתוכלי להשתמש בה כמו אפליקציה אמיתית — עם שמירה קבועה ותזכורות.

זה ייקח כ-30-45 דקות בפעם הראשונה. אחרי זה, כל שינוי עתידי יהיה הרבה יותר מהיר.

\---

## לפני שמתחילות — מה תצטרכי

* כתובת אימייל
* כרטיס אשראי **לא נדרש** — כל השירותים כאן חינמיים לשימוש אישי
* כ-45 דקות בלי הפרעות

\---

## שלב 1: יצירת חשבון GitHub (מקום לאחסן את הקוד)

GitHub הוא כמו "תיקיית Google Drive" לקוד — Vercel יקרא משם את הקוד כדי להעלות אותו.

1. גשי ל-https://github.com/signup
2. הירשמי עם האימייל שלך, בחרי סיסמה
3. אשרי את האימייל (ייכנס מייל אימות)
4. לאחר ההרשמה, לחצי על ה-**+** בפינה הימנית העליונה → **New repository**
5. תני שם ל-repository, למשל `matbagil`
6. השאירי אותו **Private** (רק את תראי את הקוד)
7. לחצי **Create repository**

השאירי את הדף הזה פתוח — נחזור אליו.

### העלאת הקוד ל-GitHub

הדרך הכי פשוטה בלי שורת פקודה:

1. בדף ה-repository החדש שיצרת, לחצי על הקישור **uploading an existing file**
2. גררי לתוך הדפדפן את **כל התיקייה `matbagil-app`** שקיבלת ממני (או את כל הקבצים שבתוכה)
3. גללי למטה, לחצי **Commit changes**

> 💡 אם הגרירה של תיקייה שלמה לא עובדת בדפדפן שלך, אפשר לדחוס אותה ל-ZIP קודם ואז לחלץ מחדש, או לבקש ממני הנחיה חלופה עם שורת פקודה.

\---

## שלב 2: יצירת פרויקט Supabase (מסד הנתונים)

Supabase הוא מסד הנתונים האמיתי שישמור את המתכונים, התפריטים וההגדרות שלך.

1. גשי ל-https://supabase.com ולחצי **Start your project**
2. הירשמי עם GitHub (הכי פשוט — לחצי "Continue with GitHub")
3. לחצי **New project**
4. תני שם לפרויקט: `matbagil`
5. בחרי **Database Password** — כתבי אותה במקום בטוח, לא תצטרכי אותה שוב בקרוב אבל שווה לשמור
6. בחרי אזור (Region) — **Europe (Frankfurt)** הכי קרוב לישראל
7. לחצי **Create new project**
8. **חכי כ-2 דקות** עד שהפרויקט מוכן (יוצג "Setting up project...")

### הרצת ה-schema (מבנה הטבלאות)

1. בתפריט השמאלי, לחצי על **SQL Editor**
2. לחצי **New query**
3. פתחי אצלך את הקובץ `supabase/schema.sql` שקיבלת ממני
4. העתיקי את **כל התוכן** שלו והדביקי בעורך ב-Supabase
5. לחצי **Run** (או Ctrl+Enter)
6. אמורה להופיע הודעת הצלחה בירוק

### הזנת המתכונים (Seed data)

1. שוב **New query** ב-SQL Editor
2. פתחי את הקובץ `supabase/seed.sql`
3. העתיקי את כל התוכן, הדביקי, לחצי **Run**
4. זה ייקח כמה שניות — 85 מתכונים נכנסים למסד הנתונים

### שליפת המפתחות (API keys)

1. בתפריט השמאלי → **Project Settings** (⚙️ למטה) → **API Keys** (לא "General" — זה נותן רק שם ואזור, לא מפתחות)
2. תמצאי שם 3 ערכים שנצטרך בהמשך — שימי לב, Supabase קוראת להם היום בשמות מעט שונים ממה שהיה נהוג (הערכים עצמם עובדים באותה צורה):

   * **Project URL** (למעלה בעמוד — נראה כמו `https://xxxxx.supabase.co`)
   * **Publishable key**, שורת `default` (מתחיל ב-`sb\_publishable\_...`)
   * **Secret keys**, בגלילה למטה (מתחיל ב-`sb\_secret\_...` — **סודי!** אל תשתפי את זה עם אף אחד)
3. השאירי את הדף הזה פתוח בכרטיסייה נפרדת — נצטרך את הערכים בשלב 4 (טבלה מלאה שם עם ההתאמה המדויקת לכל משתנה)

\---

## שלב 3: יצירת מפתחות VAPID (לתזכורות דחיפה)

אלה לא סיסמאות או פרטים אישיים — הם שני קודים טכניים אקראיים שהאפליקציה משתמשת בהם כדי "לחתום" על התראות שהיא שולחת לטלפון שלך, כך שהדפדפן יודע שההתראה אכן מגיעה מהאפליקציה שלך. זה מנגנון אבטחה, לא איום.

**נייצר אותם כאן, ישירות, בלי להיעזר באתר חיצוני כלל** — כך שאין צורך לסמוך על שום כלי צד-שלישי:

1. גשי לאתר https://replit.com (או כל שירות דומה שמריץ קוד בדפדפן) **או**, אם יש לך גישה לטרמינל/שורת פקודה במחשב (Mac: אפליקציית Terminal, Windows: PowerShell), הריצי שם:

```
   npx web-push generate-vapid-keys
   ```

2. יופיעו לך **Public Key** ו-**Private Key** — שמרי את שניהם, נצטרך אותם בשלב הבא

> 💡 אם זה נשמע מסובך מדי — אני יכול פשוט לייצר עבורך זוג מפתחות כאן בשיחה שלנו, ולתת לך אותם ישירות להעתקה. תגידי לי ואני אכין לך אותם.

\---

## שלב 4: העלאה ל-Vercel (האירוח בפועל)

1. גשי ל-https://vercel.com/signup
2. הירשמי עם **Continue with GitHub** (אותו חשבון משלב 1)
3. אחרי ההרשמה, לחצי **Add New...** → **Project**
4. תראי רשימת ה-repositories שלך מ-GitHub — בחרי את `matbagil`
5. לחצי **Import**

### הגדרת משתני הסביבה (Environment Variables)

לפני שלוחצות Deploy, יש למלא 6 משתנים בעמוד ה-Import של Vercel, תחת הכותרת **"Environment Variables"**.

**חשוב לדעת מראש:** Supabase שינתה לאחרונה את שמות המפתחות שלה במסך הדשבורד (מ-`anon`/`service\_role` ל-`Publishable`/`Secret`) — **אבל הערכים עצמם עדיין הולכים לאותם שמות משתנים ב-Vercel**, בדיוק כפי שכתוב למטה. זה רק שינוי בשם שרואים במסך של Supabase, לא שינוי במה שצריך למלא.

\---

#### שלב א׳: איפה למצוא את כל הערכים ב-Supabase

1. בפרויקט שלך ב-Supabase, לכי לתפריט השמאלי → **Project Settings** (⚙️)
2. שם יש כמה טאבים — תלחצי על **API Keys** (לא על "General" — זה נותן רק שם ואזור, לא את המפתחות עצמם)
3. במסך הזה יש שלושה חלקים שונים שצריך להבדיל ביניהם:

|מה שרואים במסך|מה זה בפועל|הולך למשתנה|
|-|-|-|
|**Project URL** (למעלה, מתחיל ב-`https://` ומסתיים ב-`.supabase.co`)|הכתובת של מסד הנתונים שלך|`NEXT\_PUBLIC\_SUPABASE\_URL`|
|**Publishable key** → שורת `default` (מתחיל ב-`sb\_publishable\_...`)|מפתח "ציבורי", בטוח לחשיפה|`NEXT\_PUBLIC\_SUPABASE\_ANON\_KEY`|
|**Secret keys** (מתחיל ב-`sb\_secret\_...`, בגלילה למטה)|מפתח "סודי", הרשאות מלאות|`SUPABASE\_SERVICE\_ROLE\_KEY`|

> ⚠️ \*\*טעות נפוצה:\*\* אל תעתיקי את הקישור לדשבורד שלך (`https://supabase.com/dashboard/project/...`) — זה לא ה-Project URL. ה-URL הנכון מסתיים ב-`.supabase.co` בלבד, בלי `supabase.com/dashboard` בהתחלה.

\---

#### שלב ב׳: טבלת כל 6 המשתנים

|#|שם המשתנה (להעתיק בדיוק)|מאיפה להעתיק את הערך|
|-|-|-|
|1|`NEXT\_PUBLIC\_SUPABASE\_URL`|Supabase → API Keys → **Project URL**|
|2|`NEXT\_PUBLIC\_SUPABASE\_ANON\_KEY`|Supabase → API Keys → **Publishable key** (`sb\_publishable\_...`)|
|3|`SUPABASE\_SERVICE\_ROLE\_KEY`|Supabase → API Keys → **Secret keys** (`sb\_secret\_...`)|
|4|`NEXT\_PUBLIC\_VAPID\_PUBLIC\_KEY`|המפתח הציבורי ששלחתי לך בהודעה נפרדת בשיחה|
|5|`VAPID\_PRIVATE\_KEY`|המפתח הפרטי ששלחתי לך באותה הודעה|
|6|`CRON\_SECRET` fsjlsdfkj8798w7ldj0s9sfflgsaljskl|כל מחרוזת אקראית ארוכה שאת בוחרת — למשל `matbagil-secret-8k2m9x`|

\---

#### שלב ג׳: איך למלא בפועל ב-Vercel

לכל אחד מ-6 המשתנים בטבלה:

1. בשדה **Key** — מדביקים את **שם המשתנה בדיוק** מהעמודה הראשונה (למשל `NEXT\_PUBLIC\_SUPABASE\_URL`) — בלי רווחים, בלי גרשיים, בלי שום תו נוסף בסוף
2. בשדה **Value** — מדביקים את **הערך האמיתי** שהעתקתי מ-Supabase/מהמפתחות ששלחתי (לא את הטקסט התיאורי מהטבלה!)
3. משאירים את **Environments** על ברירת המחדל — **"Production and Preview"** (אין צורך לשנות)
4. לוחצים **Add** או **+ Add More** כדי לעבור למשתנה הבא

חוזרים על התהליך עד שכל 6 המשתנים נמצאים ברשימה.

**איך לוודא שהכל נכון לפני שממשיכים:** לכל משתנה, בודקים:

* ✅ שם המשתנה תואם בדיוק לעמודה הראשונה בטבלה (אפשר להעתיק-הדבק ישירות מכאן)
* ✅ הערך הוא באמת מה שהועתק מ-Supabase/מההודעה שלי — לא כתובת לדשבורד, לא טקסט הסבר

### Deploy

1. אחרי מילוי כל 6 המשתנים, לחצי **Deploy**
2. חכי כ-2-3 דקות — Vercel בונה את האפליקציה
3. כשמסתיים, יופיע 🎉 עם קישור לאתר שלך — משהו כמו `matbagil.vercel.app`

**זהו — האפליקציה שלך חיה!**

\---

## שלב 5: כניסה ראשונה

1. פתחי את הקישור שקיבלת (`matbagil.vercel.app`) בטלפון או במחשב
2. הזיני את האימייל שלך
3. ייכנס מייל עם קישור כניסה — לחצי עליו
4. את בפנים!

### הוספה למסך הבית (כדי שזה ירגיש כמו אפליקציה)

**באייפון (Safari):**

1. פתחי את האתר ב-Safari
2. לחצי על כפתור השיתוף (הריבוע עם החץ)
3. גללי ובחרי **הוסף למסך הבית**

**באנדרואיד (Chrome):**

1. פתחי את האתר ב-Chrome
2. לחצי על שלוש הנקודות בפינה
3. בחרי **הוסף למסך הבית** / **התקן אפליקציה**

עכשיו יש לך אייקון על מסך הבית כמו כל אפליקציה אחרת.

### הפעלת תזכורות

1. בתוך האפליקציה, כשתתבקשי לאשר התראות — לחצי **אשר**
2. מכאן האפליקציה תדע לשלוח לך תזכורות אמיתיות (קניות, הפשרה) בזמנים הנכונים

\---

## מה קורה עכשיו כשרוצים לשנות משהו?

כל פעם שנרצה לעדכן את האפליקציה (עוד מתכונים, תיקון, פיצ'ר חדש) — אני אעדכן את הקבצים, ואת:

1. פותחת את ה-repository ב-GitHub
2. מעלה את הקבצים המעודכנים (אותו תהליך גרירה כמו בשלב 1)
3. Vercel מזהה את השינוי אוטומטית ומפרסם גרסה חדשה תוך כ-1-2 דקות — בלי שתצטרכי לעשות שום דבר נוסף

\---

## פתרון בעיות נפוצות

**"Build failed" ב-Vercel** — לרוב זה אומר שמשתנה סביבה חסר או שגוי. בדקי שכל 6 המשתנים משלב 4 מולאו נכון, בלי רווחים מיותרים.

**לא מקבלת מייל כניסה** — בדקי בספאם. אם עדיין לא מגיע, ודאי שכתובת האימייל הוזנה נכון.

**האפליקציה עולה אבל ריקה מנתונים** — סימן שה-seed.sql לא רץ בהצלחה. חזרי לשלב 2, הריצי אותו שוב ב-SQL Editor של Supabase ובדקי אם מופיעה שגיאה.

**תזכורות לא מגיעות** — ודאי שאישרת הרשאת התראות בדפדפן/באפליקציה, ושעברו לפחות 24 שעות (ה-cron רץ פעם ביום).

\---

אם משהו נתקע באמצע — תעצרי ותשלחי לי בדיוק מה קרה (צילום מסך של השגיאה עוזר מאוד), ונפתור את זה ביחד.

